1.运行
docker run -itd --name mongo -p 27017:27017 mongo --auth
2.创建admin用户
docker exec -it mongo mongo admin
db.createUser({ user:'admin',pwd:'123456',roles:[ { role:'userAdminAnyDatabase', db: 'admin'}]});#在用户标创建一个用户表的超级管理员帐号
3.创建普通用户
db.auth('admin','123456');#刚刚添加需要验证一下权限，才可以下面操作。
db.createUser( {user: "asi",pwd: "123456",roles: [ { role: "root", db: "admin" } ]});#创建一个root权限的用户，具体权限，可以参考《mongo权限相关》
然后就搞定。可以用navicat,连接了。