y复制一行，或者多行 y1,y2,y3
$ → 到行尾
dd 删除一行
u撤销编辑
p粘贴
i进入编辑
v进入查看模式 (用于多选)
set nu显示行号
x → 删当前光标所在的一个字符。
saveas <path/to/file> → 另存为
/super从上往下搜索
？super从下往上搜索
最快的方式是让光标停留在想要查找的单词的任意一个字母上面， 然后输入Shift + *  ，即可快速选中该单词，并且可以通过n进行上一个或下一个的匹配。